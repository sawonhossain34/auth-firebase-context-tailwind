/***
 * Context API
 * 1 create context api (export).
 * 2 create provider.
 * 3 set context value.
 * 4 to access the value : useContext hook.
 */

/**
 * 1 main.jsx set AuthProvider
 * 2 inside provider access children props and then use
 */

/**
 * 1 break down create user function
 * 2 auth provider will have auth and the pass email and password from register
 */